# プロジェクト概要

ClaudeCodeの基本的な使い方を習得することを目的とした練習プロジェクト。
ブラウザで遊べる〇×ゲーム(三目並べ)をHTML/CSS/JavaScriptで作成する。

# 目的

- ClaudeCodeとの基本的なやり取り(指示・修正・確認)に慣れる
- ファイル作成、編集、動作確認の一連の流れを体験する

# 技術スタック

- HTML / CSS / JavaScript(素のJS、フレームワーク不使用)
- ビルドツールなし。ブラウザで `index.html` を直接開いて動作確認する

# 機能要件

- 3x3のマス目で〇と×を交互に置ける
- 縦・横・斜めのいずれかが揃ったら勝敗を表示する
- 全マス埋まって勝敗がつかない場合は引き分けを表示する
- 「リセット」ボタンで最初からやり直せる

# コーディング方針

- シンプルさ優先。過剰な抽象化やライブラリ導入はしない
- コメントは必要最小限(自明でない意図がある場合のみ)
